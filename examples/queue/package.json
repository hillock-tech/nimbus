{
  "name": "queue-example",
  "version": "1.0.0",
  "description": "Nimbus example: SQS queue with Lambda worker",
  "private": true,
  "scripts": {
    "deploy": "npx nimbus deploy",
    "destroy": "npx nimbus destroy --project queue-app --region us-east-1 --force"
  },
  "dependencies": {
    "@hillock-tech/nimbus-js": "file:../..",
    "@aws-sdk/client-sqs": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "nanoid": "^3.3.11"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "@types/node": "^22.10.2"
  }
}
