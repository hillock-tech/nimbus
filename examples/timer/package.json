{
  "name": "nimbus-timer-example",
  "version": "1.0.0",
  "description": "Nimbus Timer example - scheduled Lambda functions",
  "main": "index.ts",
  "scripts": {
    "deploy": "npx nimbus deploy",
    "destroy": "npx nimbus destroy --project timer-app --region us-east-1"
  },
  "dependencies": {
    "@hillock-tech/nimbus-js": "file:../.."
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}