{
  "name": "nimbus-example-sql",
  "version": "1.0.0",
  "private": true,
  "description": "Aurora DSQL database example using Nimbus",
  "scripts": {
    "deploy": "npx nimbus deploy index.ts",
    "destroy": "npx nimbus destroy --project sql-app --region us-east-1"
  },
  "dependencies": {
    "@aws-sdk/dsql-signer": "^3.919.0",
    "@hillock-tech/nimbus-js": "file:../..",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "tsx": "^4.7.0",
    "typescript": "^5.5.0"
  }
}
